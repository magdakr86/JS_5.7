doctype html
html
    include ./second-template.pug
    body
        div
            if(login)
                h1 Witaj, #{login}
                div.text-center
                |   Logowanie zakończyło się pomyślnie.
            else
                div.text-center
                    |   Logowanie zakończyło się niepowodzeniem.
                    |
                    a(href="/") Spróbuj ponownie.