section.contact
    .form-component
        div
            h2.form-header Witaj!
            p.form-desc
                | Aby zalogować się, uzupełnij poniższe dane:
        form(action='http://localhost:8080/auth/google', method='GET')
            .form-group
                label(for='login') Twój login:
                input#login.form-control(type='text', name='login', aria-describedby='emailHelp', placeholder='Twój email')
            .form-group
                label(for='password') Hasło:
                input#password.form-control(type='password', name='password', placeholder='Twoje hasło')
            button.btn.btn-primary(type='submit') Zaloguj się
            div(style='clear:both;')